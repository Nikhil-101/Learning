export const add = (a, b) => {
  return a+b;
}
export const sub = (a, b) => {
  return a-b;
}
export const mul = (a, b) => {
  return a*b;
}
export const div = (a, b) => {
  return a/b;
}

export const PI = 3.125;

// all these above are named exports

// also this one
// export { add, sub, mul, div, PI };

// default export : only one is allowed
// export default add;